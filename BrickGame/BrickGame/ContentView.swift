import SwiftUI

struct ContentView: View {
    var body: some View {
        GameBoardView()
    }
}

#Preview {
    ContentView()
}

// This file is the app's main screen entry that hosts the game board view.
